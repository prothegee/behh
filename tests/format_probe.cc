// throwaway probe to test the format workflow, delete after verifying.
// this is intentionally mis-formatted so clang-format has something to fix.
#include <iostream>

int main(){
int   x=1 ;
        if( x==1 ){std::cout<<"probe "<<x<<"\n";}

  return 0;
}
